{
  "phase": "proposal",
  "requirements": {
    "problem": "Replace with a solution-free problem statement",
    "users": ["primary-user"],
    "critical_use_cases": ["critical-path"],
    "constraints": [{"id": "deployment", "text": "single-host deployment"}],
    "quality_attributes": [{
      "id": "latency",
      "scenario": "one critical request under declared load",
      "target": "p95 < 500 ms",
      "evidence_state": "PLANNED"
    }],
    "capacity_estimates": [{
      "id": "peak-request-rate",
      "metric": "critical request rate",
      "value_range": "UNKNOWN",
      "unit": "requests/minute",
      "unknown_reason": "No production traffic exists yet; measure during walking skeleton rehearsal",
      "evidence_state": "UNKNOWN"
    }]
  },
  "state_model": {
    "current_state": "greenfield/no deployed system",
    "target_state": "single deployable with explicit API and data-store boundaries",
    "gaps": ["traffic is unknown until rehearsal"],
    "evidence_state": "PLANNED"
  },
  "fitness_functions": [{
    "id": "ff-latency",
    "quality_attribute_id": "latency",
    "signal": "p95 latency from request receipt to durable response",
    "threshold": "< 500 ms under declared capacity range",
    "verification": "walking skeleton timing probe with recorded command/output",
    "evidence_state": "PLANNED"
  }],
  "options": [
    {
      "id": "modular-monolith",
      "summary": "One deployable with explicit internal modules",
      "tradeoffs": ["simple operations", "shared failure domain"],
      "rejection_conditions": ["independent scaling becomes measured requirement"],
      "reversal_cost": "moderate",
      "action_ids": ["create-module-boundaries", "create-api-contract"],
      "constraint_results": [{
        "constraint_id": "deployment",
        "status": "PASS",
        "evidence": "one process and one deployment artifact"
      }],
      "fitness_results": [{
        "fitness_id": "ff-latency",
        "status": "PASS",
        "evidence": "no network hop between API and core module in the proposed skeleton"
      }],
      "migration": {
        "applicable": false,
        "not_applicable_reason": "greenfield system with no existing consumers"
      }
    },
    {
      "id": "two-services",
      "summary": "Two independently deployable services",
      "tradeoffs": ["independent scaling", "network and operations cost"],
      "rejection_conditions": ["single operator cannot support two deployments"],
      "reversal_cost": "hard",
      "action_ids": ["create-service-a", "create-service-b", "create-service-contract"],
      "constraint_results": [{
        "constraint_id": "deployment",
        "status": "PASS",
        "evidence": "both services run on the declared single host"
      }],
      "fitness_results": [{
        "fitness_id": "ff-latency",
        "status": "PASS",
        "evidence": "same-host service call remains within the declared timing probe scope"
      }],
      "migration": {
        "applicable": false,
        "not_applicable_reason": "greenfield system with no existing consumers"
      }
    }
  ],
  "recommendation": {
    "option_id": "modular-monolith",
    "rationale": "Meets current constraints with the smallest justified operational surface"
  }
}
